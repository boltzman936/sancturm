// TypeScript types for the "resources" feature (Notes & Lab and PYQs
// share the same underlying `resources` table).
export type {
  Resource,
  ResourceType,
  ResourceSection,
  ResourceStatus,
  Subject,
  SubjectStructureConfig,
} from "@/types/database";
