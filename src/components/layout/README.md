# Layout components

App chrome that wraps every page inside `(app)` — not tied to any one
feature. This is where the real Sidebar, CommandPalette (Ctrl+K), and
BranchSwitcher will live once we design them; right now their job is
being done by the plain nav inside `src/app/(app)/layout.tsx` as a
placeholder.
